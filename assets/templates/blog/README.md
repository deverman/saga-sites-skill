# Blog Template Notes

Use the minimal-site template and add:

- `content/posts/`
- paginated `/writing/`
- full tag/category archives by default
- Atom and JSON feeds
- post metadata reports
- SEO tests for title, description, canonical, social image, feeds, and JSON-LD
