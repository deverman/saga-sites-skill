# WordPress Migration Template Notes

Use the minimal-site template and add:

- WordPress-compatible `path:` preservation
- `/wp-content/uploads/...` media preservation
- taxonomy slug compatibility
- `_redirects` generation or checked redirect map
- SiteDoctor reports for paths, titles, descriptions, tags, and images
- full archive validation before deploy
